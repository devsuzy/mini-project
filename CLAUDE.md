# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

카카오톡 그룹채팅 CSV 를 올리면 **답변이 빠진 질문·공지거리·이슈 신호·반복 질문** 을 뽑아 방장에게 액션 아이템으로 돌려주는 로컬 웹 도구. 로컬 LLM(Ollama)만 쓰므로 API 키도 과금도 없고, 남의 대화가 머신 밖으로 나가지 않는다.

## Commands

```bash
npm start                              # http://localhost:5178
PORT=3000 npm start                    # 포트 변경
OLLAMA_MODEL=qwen2.5:14b npm start     # 모델 교체
OLLAMA_HOST=http://host:11434 npm start

./test/run.sh                          # 파서·필터·탐지·병합 단위 테스트 (모델 불필요, 1초)
node test/e2e.js                       # 허구 샘플로 실제 모델까지 태우기 (~45초)
node test/e2e.js <csv> <내이름>         # 다른 파일로

node --check server.js                 # 문법 검사 (빌드 단계 없음)
```

`npm install` 은 불필요하다 — **의존성이 0개다.** Node 20.12+ (전역 `fetch` 와 `--env-file-if-exists` 필요). `.env` 는 `dotenv` 없이 Node 내장 플래그로 읽는다. 패키지를 추가하기 전에 내장 모듈로 되는지 먼저 확인할 것.

Ollama 는 `brew services start ollama` 로 상시 실행 중이어야 한다.

## Architecture

3단 홉이고, 브라우저는 Ollama 와 직접 통신하지 않는다.

```
브라우저 (index.html)              server.js :5178        Ollama :11434
 CSV 파싱 → 필터 → 청킹  ─청크씩→   프롬프트 조립  ──→   qwen2.5:7b
 진행률·병합·렌더      ←────────  {요약,질문,공지,이슈,FAQ}
```

**CSV 파싱·노이즈 필터·질문 탐지·청킹·병합은 전부 브라우저에서 한다.** 2만 줄 파일을 통째로 POST 하지 않기 위해서다. `server.js` 는 정적 서빙과 Ollama 프록시만 한다.

| 라우트 | 역할 |
|---|---|
| `GET /` | `index.html` 서빙. 요청마다 디스크에서 읽으므로 **HTML 수정은 서버 재시작 불필요** |
| `GET /api/config` | `{model, running, hasModel}` — UI 가 준비 안내 배너를 띄울지 결정 |
| `POST /api/analyze` | 청크 1개 → `{summary, unanswered, notices, issues, faqs, dropped}` |
| `POST /api/reduce` | 청크가 2개 이상일 때 요약·FAQ 를 한 번 더 합침 |

### index.html 의 두 스크립트 블록

- `<script id="core">` — **순수 로직. DOM 을 만지지 않는다.** `test/extract-core.sh` 가 이 블록만 뽑아 Node 로 돌린다. 여기에 `document`/`window` 를 넣으면 테스트가 깨진다.
- `<script id="ui">` — DOM 바인딩.

추출 스크립트의 sed 범위에 `^` 앵커가 있는 이유: 앵커가 없으면 **주석 안에 적힌 `<script id="core">` 문자열에도 범위가 다시 걸려** UI 블록까지 통째로 딸려온다. 실제로 그렇게 깨진 적이 있어서 `test/parser.test.js` 첫 섹션이 이걸 감시한다.

## 카톡 CSV 의 함정 (전부 실제 파일에서 확인됨)

헤더는 `Date,User,Message` 다. 그 위에 네 가지가 얹혀 있다.

1. **UTF-8 BOM (`EF BB BF`)** — 안 벗기면 첫 컬럼명이 `Date` 가 아니라 `﻿Date` 가 되어 헤더 매칭이 조용히 실패한다.
2. **따옴표 안 개행** — 링크 목록이 든 메시지 하나가 물리적으로 12줄을 차지한다. `text.split('\n')` 파싱은 **반드시 깨진다.** 그래서 `parseCsv()` 가 RFC 4180 상태 머신이다.
3. **Date·User 가 빈 행** — `,"","메시지가 삭제되었습니다."`. 버리지 말고 **직전 메시지의 시각을 승계**시킨다. 버리면 대화 흐름에 구멍이 나서 "질문 다음에 뭐가 왔는지" 판정이 틀어진다.
4. **높은 노이즈 비율** — 실제 파일에서 30건 중 16건이 `사진`/`이모티콘`/`파일:`/`ㅋㅋ`/삭제됨이었다. 절반이 넘는다.

노이즈 필터는 **되돌릴 수 있어야 한다.** 무엇이 왜 걸러졌는지 UI 에서 펼쳐볼 수 있다 — 안 그러면 오탐을 영영 발견 못 한다.

## 규칙 + LLM 하이브리드가 핵심이다

7B 모델에게 "답 안 된 질문 찾아줘" 를 통으로 시키면 정확도가 크게 떨어진다. 그래서 **규칙이 후보를 확정하고 모델은 판정·문장 다듬기만 한다.**

- `findUnansweredCandidates()` — 질문 어미로 후보를 잡고, 이후 응답을 3분류한다.
  `answered`(다른 사람의 실질 텍스트 응답 → 제외) / `media`(미디어로만 응답 → 불확실, 모델이 판정) / `none`(무응답이나 리액션뿐).
  **윈도우는 2시간/5개다.** 30분으로 잡았더니 답변된 질문까지 미답변으로 잡혔다 — 업무 채팅은 답이 한참 뒤에 온다.
  `넵`/`감사합니다`/이모티콘은 응답으로 치지 않는다(`RE_FILLER`).
- `findRepeatedQuestions()` — 문자 바이그램 자카드로 유사 질문을 전이적으로 묶는다(임계 0.28).
  **어미 제거(`normQuestion`)가 핵심이다.** 안 벗기면 `"회식 장소 알려주실 수 있나요?"` 와 `"서버 접속 정보 알려주실 수 있나요?"` 가 공통 어미 때문에 같은 질문으로 묶인다.
  같은 사람이 두 번 물은 건 FAQ 가 아니라 무시당한 질문이라 제외한다.

`server.js` 의 `tidy()` 가 모델 출력을 이 후보 목록 안으로 **강제 제한**한다. 목록에 없는 질문을 지어내면 버린다.

## 프롬프트와 스키마는 한 몸이다

`AI-new-project` 에서 넘어온 교훈에 이 프로젝트에서 새로 얻은 것을 더했다.

- **Ollama 는 JSON 스키마의 `description` 을 모델에게 전달하지 않는다.** 스키마는 GBNF 로 컴파일돼 *출력 형태*만 강제한다. 필드 의미는 반드시 `systemPrompt()` 에 있어야 한다. (호스팅 API 는 정반대다 — 다른 프로바이더에서 코드를 옮길 때 조용히 깨지는 지점.)
- **`properties` 의 순서가 곧 생성 순서다.** `summary` 를 맨 앞에 뒀더니 모델이 요약을 쓰고 나서 "할 말 다 했다" 는 듯 뒤쪽 배열을 비워 보냈다 — `issues` 와 `faqs` 가 통째로 빈 채 나왔다. **배열 넷을 먼저 채우게 하고 `summary` 를 맨 뒤로 옮겨서 고쳤다.** 순서를 되돌리기 전에 이 문장을 볼 것.
- **예시는 자리표시자(`<사람 이름>`)로 쓰고 "그대로 출력하지 말라"고 명시한다.** 현실적인 예시를 넣으면 모델이 대화 대신 예시를 베껴서 없는 항목을 만들어낸다.
- **`num_ctx: 16384` 를 명시한다.** Ollama 기본값은 2048 이고, 넘치면 **에러 없이 잘라낸다.**
- **`temperature: 0`** — 추출 작업이라 같은 입력에 같은 결과가 나와야 한다.
- **제외 기준을 적는 것이 포함 기준보다 중요하다.** `issues` 에 "힘들다/바쁘다" 를 넣으라고만 썼더니 실제 파일에서 `"이번주는 넘 바빠서 다음주부터 조사하려고요"`(단순 일정 조율)와 `"백화점 야간에 작업했어요"`(근황 공유)를 피로 호소로 잡아 오탐 3건이 나왔다. **"넣지 않는 것" 목록을 명시하자 오탐이 0이 되고, 덤으로 비어 있던 `notices` 까지 살아났다.**

필드를 추가할 때는 네 곳을 함께 고쳐야 한다:
1. `ANALYZE_SCHEMA` (`server.js`) — 형태와 **순서**
2. `systemPrompt()` (`server.js`) — 의미
3. `tidy()` (`server.js`) — 검증
4. `render()` / `copyMd` (`index.html`) — 표시

## 청킹과 병합

- **`CHUNK_CHARS`(index.html) 와 `num_ctx`/`MAX_INPUT_CHARS`(server.js) 는 한 몸이다.** 한쪽만 바꾸면 조용히 잘린다.
  실측: 시스템 프롬프트 2,683자 + 청크 6,475자 = **6,583 / 16,384 토큰**, 출력 여유 9,801 토큰 (한국어 자당 0.72 토큰).
  여유는 남지만 7B 는 컨텍스트가 길수록 느려지고 뒤쪽 필드를 성의 없이 채운다. 늘리기 전에 실제로 재볼 것.
- 청크는 **날짜 경계를 우선** 지켜 자른다. 하루가 예산을 넘으면 그 안에서 다시 자르되 앞 청크의 마지막 5개를 겹쳐 넘긴다.
- **`mergeResults()` 의 중복 제거가 필수다.** 청크마다 모델을 따로 부르므로 여러 날 반복되는 이슈는 청크 수만큼 중복돼 돌아온다. 실측: 4청크 대화에서 이슈가 24건 나왔는데 서로 다른 내용은 3가지뿐이었다. 유사도 0.5 로 묶고, 겹치면 가장 높은 severity 를 남기고 반복 구간 수(`_n`)를 센다.

## 성능 (실측)

로컬 `qwen2.5:7b`, M 시리즈 맥 기준.

| 규모 | 청크 | 소요 |
|---|---|---|
| 30건 (실제 파일) | 1 | 30초 |
| 34건 (샘플) | 1 | 44초 |
| 396건 (12일치) | 4 | **236초** |

청크당 50~60초다. 프롬프트를 조이기 전에는 청크당 최대 180초까지 늘어졌다 — 출력이 장황하면 그만큼 느려진다. `estimateSeconds()` 가 75초/청크로 넉넉히 잡는 이유다 — 낙관적으로 잡으면 사용자가 멈춘 줄 알고 새로고침한다. 2만 메시지 전체는 30분이 넘으므로 **기간 선택은 선택 기능이 아니다.** 데이터가 3청크를 넘으면 UI 가 기본값을 최근 7일로 잡는다.

## 프라이버시

- `server.listen(PORT, '127.0.0.1')` — 기본값(`0.0.0.0`)이면 같은 Wi-Fi 의 다른 기기에서 남의 카톡 대화가 열린다. UI 의 "밖으로 나가지 않습니다" 문구와 정면으로 어긋나므로 되돌리지 말 것.
- **실제 카톡 내보내기는 커밋 금지.** `.gitignore` 가 `KakaoTalk_Chat_*` 등을 막는다. 개발·테스트에는 허구 데이터인 `sample-chat.csv` 를 쓴다. 이 파일에는 BOM·멀티라인·삭제행·노이즈·미답변 질문 3건·반복 질문 3건이 일부러 심어져 있다.
- 모델 출력은 `textContent` 로만 DOM 에 넣는다 (`el()`). `innerHTML` 로 바꾸지 말 것.

## 검증

**테스트 스위트는 `./test/run.sh` 뿐이고 모델을 부르지 않는다** (파서·필터·탐지·병합만). 프롬프트를 고치면 이 테스트는 아무것도 잡아주지 못한다 — `node test/e2e.js` 로 두 파일(허구 샘플, 실제 파일) 모두 눈으로 확인해야 한다.

합격선으로 삼는 케이스:
- 실제 파일의 11:03 `"설치하는 장비 리스트, 스펙, 규격, 제품명 엑셀시트 구할 수 있을까요?"` 가 **미답변으로 잡혀야 한다** (그 뒤로 답이 없다).
- 같은 파일의 `"프로세스 는 연구 하고 있나요"` 는 59분 뒤 답변이 있으므로 **잡히면 안 된다.**
- 실제 파일에서 이슈 신호가 **0건이어야 한다** (오탐 방지 대조군).

```bash
# Ollama 다운 상태 재현 — 죽은 포트를 가리켜 별도 인스턴스 기동
PORT=5188 OLLAMA_HOST=http://127.0.0.1:19999 npm start
```

## 알려진 한계 (미해결)

| 위치 | 문제 |
|---|---|
| 프롬프트 전반 | 7B 모델이라 **같은 정보가 실행마다 다른 필드에 담긴다.** 계약서 위치가 어떤 실행에서는 `faqs.answer` 로, 어떤 실행에서는 `notices` 로 갔다. `temperature:0` 이어도 프롬프트가 바뀌면 흔들린다. 필드 간 이동은 정보 손실이 아니지만 회귀 테스트를 어렵게 만든다 |
| `parseCsv()` | 카톡 **txt** 내보내기(윈도우·모바일)는 지원하지 않는다. CSV 전용이다 |
| `answerState()` | 미디어로만 응답한 경우를 `uncertain` 으로 넘기는데, UI 가 이 표시를 아직 시각적으로 구분해 보여주지 않는다 |
| 청크 경계 | 질문이 청크 끝에 걸리면 답변이 다음 청크에 있어도 미답변 후보 판정은 옳게 나온다(전체 타임라인으로 판정하므로). 다만 모델은 청크 안만 보므로 `action` 문장이 맥락을 놓칠 수 있다 |
| `systemPrompt()` | **action 의 주어가 방장이 아닌 질문자로 새는 일이 있다.** 12건 중 3건에서 `"박서연에게 ~해달라고 요청하기"` 처럼 나왔다. 프롬프트에 "질문한 사람을 주어로 쓰면 틀린 것" 이라고 못박아도 7B 는 완전히 지키지 못한다 |
| `mergeResults()` | 중복 제거 로직은 단위 테스트로만 검증됐다. 큰 파일 E2E 에서는 이슈가 0건으로 나와 실제로 합쳐지는 경로를 타지 못했다 |
| 취소 | 분석 시작 후 중단할 방법이 없다. 4청크면 4분간 기다려야 한다 |
